apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-c
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-c
  template:
    metadata:
      labels:
        app: app-c
    spec:
      containers:
        - name: app-c
          image: <ECR_IMAGE_APP_C>
          ports:
            - containerPort: 5000

---

apiVersion: v1
kind: Service
metadata:
  name: app-c
spec:
  selector:
    app: app-c
  ports:
    - port: 80
      targetPort: 5000
  type: ClusterIP